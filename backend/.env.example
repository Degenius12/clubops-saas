# ClubOps Backend Environment Variables
# Copy these to your Vercel project settings

# Database (you'll need to create a database first)
DATABASE_URL="postgresql://username:password@hostname:port/database"

# JWT Secret (generate a random string)
JWT_SECRET="clubops-super-secret-jwt-key-2024-make-this-very-long-and-random-please"

# Environment
NODE_ENV="production"

# Frontend URL (will be set after frontend deployment)
FRONTEND_URL="https://your-frontend-app.vercel.app"

# Optional: Stripe (for payments later)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Redis (optional, for caching)
REDIS_URL="redis://localhost:6379"
